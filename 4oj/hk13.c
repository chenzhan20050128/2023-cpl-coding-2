#include <stdio.h>
#include <string.h>

int can_concatenate(char a[], char b[], char c[])
{
    int alen = strlen(a);
    int blen = strlen(b);
    int clen = strlen(c);
    int flag = 1;
    for (int i = 0; i <= clen - 1; i++)
    {
        if (a[i] == c[i] && strcmp())
        {
            /* code */
        }
    }
}