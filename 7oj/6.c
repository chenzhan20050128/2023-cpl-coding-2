#include<stdio.h>
#include <stding.h>
#include <math.h>
int k;
int va;
int vb;
int vc;
int a,b,c;
int a0,b0,c0;
int solve(int now_a,int now_b,int now_c){
    if (now_a == 0){
        //now_c = c_k;
        a
    }
    else if (now_b == 0){

    }
    else if (now_c == 0){

    }
}

int main(){
    scanf("%d %d %d %d %d %d %d %d %d %d",&k,&va,&vb,&vc,&a,&b,&c,&a0,&b0,&c0);

}