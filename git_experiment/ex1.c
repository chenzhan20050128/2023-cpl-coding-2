#include <stdio.h>

int main()
{
    int a, b;
    scanf("%d %d", &a, &b);
    for (int i = a; i <= b; i++)
    {
        printf("%d ", i);
    }
    return 0;
    // modify in 16:32
    // version 1;
}